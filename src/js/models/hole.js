/// <reference path="../init.js" />

(function(app){

    // namespace
    app.models = app.models || {};

    // model
    app.models.hole = function(number, par, si){

        this.number = number;
        this.par = par;
        this.si = si;
        
    }
    
})(__ || {});